<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:RandomStart.PageModels;assembly=RandomStart"
             x:Class="RandomStart.Pages.PropertyPage">
    <ContentPage.BindingContext>
        <pageModels:PropertyPageModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding MinimumDelayLabel}" />
            <Label Text="{Binding StartWindowLabel}" />
            <Label Text="{Binding StartingSoundLabel}" />
            <Label Text="{Binding StartedSoundLabel}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>