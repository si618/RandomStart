<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RandomStart.Pages.StartPage"
             BackgroundColor="Black">
    <ContentPage.Content>
        <Button 
            Text="{Binding StartText}" Command="{Binding Start}"
            WidthRequest="200" HeightRequest="200" BorderRadius="100" 
            HorizontalOptions="Center" VerticalOptions="Center"
            BackgroundColor="{Binding StartColour}">
        </Button>
    </ContentPage.Content>
</ContentPage>